Documents: [
  .pdf, .docx, .doc,
  .txt, .rtf, .odt,
  .xlsx, .xls, .pptx,
  .ppt, .md, .tex
]

Images: [
  .jpg, .jpeg, .png,
  .gif, .bmp, .tiff,
  .tif, .webp, .svg,
  .ico, .heic, .eps
]

Audio: [
  .mp3, .wav, .wv,
  .aac, .ogg, .m4a,
  .wma, .opus, .flac
]

Video: [
  .mp4, .avi, .mkv,
  .mov, .wmv, .flv,
  .webm, .m4v, .3gp
]

Archives: [
  .zip, .rar, .7z,
  .tar, .gz, .bz2,
  .xz, .tgz, .iso
]

Executables: [
  .exe, .msi, .sh,
  .bat, .cmd, .ps1,
  .bin, .deb, .rpm,
  .run, .elf, .appimage
]

Code: [
  .py, .js, .html,
  .css, .cpp, .c,
  .h, .java, .go,
  .rs, .ts, .json,
  .xml, .yaml, .yml,
  .php, .kt, .swift
]

Ebooks: [
  .epub, .mobi, .azw3,
  .fb2, .djvu
]
